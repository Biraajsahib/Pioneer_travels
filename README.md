# Pioneer Travels (Next.js)

This is a minimal Next.js app containing the `PioneerTravels` client component you provided.

Quick start:

```bash
npm install
npm run dev
```

Open http://localhost:3000 to view.
